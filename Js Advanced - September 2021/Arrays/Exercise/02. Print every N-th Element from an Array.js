function solve(arr, step) {
    let result = []
    for (let i = 0; i < arr.length; i += step) {
        result.push(arr[i])
    }
    console.log(result)
}
solve(['dsa',
'asd', 
'test', 
'tset'], 
2

)